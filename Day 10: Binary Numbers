#include <bits/stdc++.h>
using namespace std;

int main()
{ 
    int number;
    int count = 0;
    cin >> number;
     while (number!=0){
         number = number & (number>>1);
         count++;
     }
    cout << count;

    return 0;
}
